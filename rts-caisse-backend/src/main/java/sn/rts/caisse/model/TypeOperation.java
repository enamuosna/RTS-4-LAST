package sn.rts.caisse.model;

/**
 * Sens d'une opération de caisse.
 */
public enum TypeOperation {
    /** Encaissement : argent qui entre en caisse. */
    ENTREE,

    /** Décaissement : argent qui sort de la caisse. */
    SORTIE
}
