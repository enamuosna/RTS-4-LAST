# ==============================================================
# RTS Caisse - Guichet (client JavaFX)
# Fichier filtré par maven-resources-plugin au moment du build :
# les ${...} sont remplacés par les valeurs des <properties> du pom.xml
# ==============================================================

# URL du backend Spring Boot (par défaut, peut être surchargée par
# le profil Maven actif ou par -Dbackend.url=... au build)
backend.url=http://localhost:8090/api

# Métadonnées (consultées via Manifest dans le code Java)
app.version=1.0.0
app.build.timestamp=2026-05-13T12:43:52Z
app.name=rts-caisse-guichet
